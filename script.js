// =============================================================
// YOUR BOOK REVIEWS
// Edit this array to add, remove, or update reviews. The page
// re-renders automatically based on what's here.
// =============================================================
const books = [
  {
    title: "The Brothers Karamazov",
    author: "Fyodor Dostoevsky",
    genre: "Classic Fiction",
    rating: 5,
    pages: 796,
    yearRead: 2024,
    review: `A novel that earns every one of its 800 pages. Dostoevsky weaves a family murder mystery together with one of literature's most serious investigations of faith, doubt, and moral responsibility.

The "Grand Inquisitor" chapter alone is worth the price of admission. Alyosha is the rare convincingly good character in fiction.`,
    favoriteQuote: "Above all, don't lie to yourself. The man who lies to himself and listens to his own lie comes to a point that he cannot distinguish the truth within him, or around him.",
    fullReview: `The Brothers Karamazov is the kind of book you finish and immediately want to start over, because you suspect — correctly — that you only caught half of what was actually happening.

## The plot, briefly

Three brothers and their loathsome father live in a provincial Russian town. Dmitri is sensual and impulsive, Ivan is intellectual and tormented, Alyosha is gentle and devout. There is a half-brother, Smerdyakov, who is something else entirely. Their father is murdered. The novel is, on its surface, a courtroom drama about who killed him.

But it is barely about that.

## What it is actually about

It's about whether a person can be good without God — and whether a person can be good with one. It's about how we inherit our parents' worst impulses and what it costs to refuse them. It's about the impossible weight of being responsible for everyone, all the time, which is the position Father Zosima asks Alyosha to live inside.

## The Grand Inquisitor

The chapter everyone talks about deserves the attention. Ivan tells Alyosha a story about Christ returning to earth during the Spanish Inquisition and being arrested by a Cardinal who explains, calmly, why the Church has had to undo most of what Christ asked of humanity. It is the most serious case against organised religion ever written, and it was written by a believer.

That is what makes Dostoevsky exhausting in the best way. He gives the devil all the best lines and then trusts you to argue back.

## Alyosha

Most "good" characters in fiction are boring or smug. Alyosha is neither. He is good the way a dog is good — present, attentive, without theory. The novel quietly suggests this is the only kind of goodness that actually helps anyone.

## Should you read it?

Yes, but not when you're tired. Give it a month. Skip the footnotes the first time through.`
  },
  {
    title: "Project Hail Mary",
    author: "Andy Weir",
    genre: "Science Fiction",
    rating: 5,
    pages: 476,
    yearRead: 2023,
    review: `Pure science-puzzle joy. Weir does what he does best — a problem-solving protagonist alone (mostly) with a wrench and a chalkboard's worth of physics.

The friendship at the center of this book is genuinely moving. Read it knowing as little as possible.`,
    favoriteQuote: "I penetrated the outer cell membrane with a nanosyringe. — You poked it with a stick. — No! I … yes. I poked it with a stick."
  },
  {
    title: "Educated",
    author: "Tara Westover",
    genre: "Memoir",
    rating: 5,
    pages: 334,
    yearRead: 2023,
    review: `A staggering memoir about growing up in a survivalist family in rural Idaho and clawing her way to a Cambridge PhD. What stays with you isn't the dramatic episodes — it's Westover's careful, almost forensic honesty about memory, family, and self-invention.`,
    favoriteQuote: "You can love someone and still choose to say goodbye to them. You can miss a person every day, and still be glad that they are no longer in your life."
  },
  {
    title: "Dune",
    author: "Frank Herbert",
    genre: "Science Fiction",
    rating: 4,
    pages: 688,
    yearRead: 2022,
    review: `An ecology textbook disguised as a space opera disguised as a religious epic. The world-building is unmatched — Arrakis feels real in a way few invented places do.

Herbert's prose can be clunky and the politics get tangled, but the vision is so complete it doesn't matter. Stop reading after book one.`,
    favoriteQuote: "I must not fear. Fear is the mind-killer. Fear is the little-death that brings total obliteration."
  },
  {
    title: "The Remains of the Day",
    author: "Kazuo Ishiguro",
    genre: "Literary Fiction",
    rating: 5,
    pages: 245,
    yearRead: 2024,
    review: `Quiet, devastating, perfectly controlled. A butler reflects on his decades of service and slowly — through what he doesn't say — reveals a life of suppressed feeling and complicity.

Ishiguro's narrator is one of the great unreliable voices in modern fiction. The final pages are unforgettable.`,
    favoriteQuote: "What is the point in worrying oneself too much about what one could or could not have done to control the course one's life took?"
  },
  {
    title: "Thinking, Fast and Slow",
    author: "Daniel Kahneman",
    genre: "Nonfiction",
    rating: 4,
    pages: 499,
    yearRead: 2022,
    review: `The definitive popular treatment of cognitive biases and dual-process theory. Some of the priming research has aged poorly post-replication-crisis, but the core ideas about System 1 / System 2 thinking still hold up and reshape how you read the news, evaluate decisions, and notice your own reasoning.`,
    favoriteQuote: "Nothing in life is as important as you think it is, while you are thinking about it."
  },
  {
    title: "Piranesi",
    author: "Susanna Clarke",
    genre: "Fantasy",
    rating: 5,
    pages: 245,
    yearRead: 2023,
    review: `A short, strange, hypnotic book about a man living alone in an infinite house full of statues and tides. To say more is to ruin it.

Clarke's prose is unlike anything else — gentle and uncanny. Read it in two sittings.`,
    favoriteQuote: "The Beauty of the House is immeasurable; its Kindness infinite.",
    fullReview: `I am going to try very hard not to spoil this book, because the experience of reading Piranesi is inseparable from the experience of slowly working out what it is.

## A man, a house, some tides

The narrator lives in a house. The house has, as far as he can tell, infinite halls. The lower halls are flooded; the upper halls hold clouds. The middle halls, where he lives, are full of statues — thousands of them, each unique, each catalogued in his journals.

He is not lonely. He has the statues. He has the birds. He has the tides, which he has learned to predict with great care. And he has The Other, a man who visits twice a week to discuss their shared research into "A Great and Secret Knowledge."

Something is wrong. The narrator does not know what.

## What Clarke is doing

This is a novel about how the stories we tell ourselves about our lives are doing more work than we realise. The narrator is, by any reasonable measure, in a terrible situation. He experiences it as wonder. The book asks, very gently, whether that's a delusion or a kind of grace — and refuses to fully answer.

It is also a novel about institutional cruelty and the long shadow of charismatic men, but you only see that on a second read.

## The prose

Clarke writes the narrator's journals in a register I have never seen before — formal, reverent, capitalised in unexpected places (the House, the Tides, the Beauty). It should be twee. It is instead the most convincing first-person voice I have read in years.

## Read it

Two sittings. No reviews beforehand. Trust me.`
  },
  {
    title: "Sapiens",
    author: "Yuval Noah Harari",
    genre: "Nonfiction",
    rating: 3,
    pages: 443,
    yearRead: 2021,
    review: `A genuinely stimulating big-picture history of humanity, but Harari is more confident than the evidence often warrants. Useful as a provocation, not as a textbook. Read it, then read a real anthropologist.`,
    favoriteQuote: "You could never convince a monkey to give you a banana by promising him limitless bananas after death in monkey heaven."
  }
];

// =============================================================
// Rendering & interaction — you shouldn't need to edit below.
// =============================================================
const grid = document.getElementById('book-grid');
const emptyState = document.getElementById('empty-state');
const searchInput = document.getElementById('search');
const genreFilter = document.getElementById('genre-filter');
const ratingFilter = document.getElementById('rating-filter');
const sortSelect = document.getElementById('sort');
const modal = document.getElementById('modal');
const modalBody = document.getElementById('modal-body');
const libraryView = document.getElementById('library-view');
const articleView = document.getElementById('article-view');
const articleBody = document.getElementById('article-body');
const prevLink = document.getElementById('prev-link');
const nextLink = document.getElementById('next-link');

function renderStars(rating) {
  const filled = '★'.repeat(rating);
  const empty = '☆'.repeat(5 - rating);
  return `<span class="stars"><span class="filled">${filled}</span><span class="empty">${empty}</span></span>`;
}

function escapeHtml(str) {
  return String(str)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

function paragraphize(text) {
  return text
    .split(/\n\s*\n/)
    .map(p => `<p>${escapeHtml(p).replace(/\n/g, '<br>')}</p>`)
    .join('');
}

function bookCard(book, index) {
  return `
    <article class="book-card" data-index="${index}" tabindex="0" role="button" aria-label="Open review of ${escapeHtml(book.title)}">
      <div class="book-cover">${escapeHtml(book.title)}</div>
      <h3 class="book-title">${escapeHtml(book.title)}</h3>
      <p class="book-author">by ${escapeHtml(book.author)}</p>
      <div class="book-meta">
        ${renderStars(book.rating)}
        <span class="genre-tag">${escapeHtml(book.genre)}</span>
      </div>
    </article>
  `;
}

function populateGenres() {
  const genres = [...new Set(books.map(b => b.genre))].sort();
  for (const g of genres) {
    const opt = document.createElement('option');
    opt.value = g;
    opt.textContent = g;
    genreFilter.appendChild(opt);
  }
}

function applyFilters() {
  const q = searchInput.value.trim().toLowerCase();
  const genre = genreFilter.value;
  const minRating = Number(ratingFilter.value);
  const sort = sortSelect.value;

  let filtered = books
    .map((b, i) => ({ ...b, _index: i }))
    .filter(b => {
      if (genre && b.genre !== genre) return false;
      if (b.rating < minRating) return false;
      if (q) {
        const hay = `${b.title} ${b.author} ${b.genre}`.toLowerCase();
        if (!hay.includes(q)) return false;
      }
      return true;
    });

  switch (sort) {
    case 'rating-desc': filtered.sort((a, b) => b.rating - a.rating); break;
    case 'rating-asc':  filtered.sort((a, b) => a.rating - b.rating); break;
    case 'title':       filtered.sort((a, b) => a.title.localeCompare(b.title)); break;
    case 'author':      filtered.sort((a, b) => a.author.localeCompare(b.author)); break;
    case 'recent':      filtered.sort((a, b) => (b.yearRead || 0) - (a.yearRead || 0)); break;
  }

  if (filtered.length === 0) {
    grid.innerHTML = '';
    emptyState.hidden = false;
  } else {
    emptyState.hidden = true;
    grid.innerHTML = filtered.map(b => bookCard(b, b._index)).join('');
  }
}

function openModal(index) {
  const book = books[index];
  if (!book) return;
  modalBody.innerHTML = `
    <h2 id="modal-title">${escapeHtml(book.title)}</h2>
    <p class="author">by ${escapeHtml(book.author)}</p>
    ${renderStars(book.rating)}
    <div class="review-meta">
      <span><strong>Genre:</strong> ${escapeHtml(book.genre)}</span>
      ${book.pages ? `<span><strong>Pages:</strong> ${book.pages}</span>` : ''}
      ${book.yearRead ? `<span><strong>Read:</strong> ${book.yearRead}</span>` : ''}
    </div>
    <div class="review-text">${paragraphize(book.review)}</div>
    ${book.favoriteQuote ? `<blockquote class="favorite-quote">"${escapeHtml(book.favoriteQuote)}"</blockquote>` : ''}
    <a href="#book/${index}" class="full-breakdown-btn" data-breakdown>See full breakdown &rarr;</a>
  `;
  modal.hidden = false;
  modal.setAttribute('aria-hidden', 'false');
  document.body.style.overflow = 'hidden';
}

function renderArticleBody(text) {
  // Lightweight markdown: ## headings, blank-line paragraphs, > blockquotes.
  const blocks = text.split(/\n\s*\n/);
  return blocks.map(block => {
    const trimmed = block.trim();
    if (!trimmed) return '';
    if (trimmed.startsWith('## ')) {
      return `<h2>${escapeHtml(trimmed.slice(3))}</h2>`;
    }
    if (trimmed.startsWith('### ')) {
      return `<h3>${escapeHtml(trimmed.slice(4))}</h3>`;
    }
    if (trimmed.startsWith('> ')) {
      return `<blockquote>${escapeHtml(trimmed.slice(2))}</blockquote>`;
    }
    return `<p>${escapeHtml(trimmed).replace(/\n/g, '<br>')}</p>`;
  }).join('');
}

function renderArticle(index) {
  const book = books[index];
  if (!book) {
    articleBody.innerHTML = `<p>Book not found. <a href="#">Back to all reviews</a>.</p>`;
    prevLink.hidden = true;
    nextLink.hidden = true;
    return;
  }

  const content = book.fullReview || book.review;
  const note = book.fullReview
    ? ''
    : `<p style="color: var(--text-muted); font-style: italic; margin-bottom: 1.5rem;">A longer breakdown for this book is coming soon — here's the short take in the meantime.</p>`;

  articleBody.innerHTML = `
    <header>
      <h1>${escapeHtml(book.title)}</h1>
      <p class="article-author">by ${escapeHtml(book.author)}</p>
      <div class="article-meta-row">
        ${renderStars(book.rating)}
        <span class="genre-tag">${escapeHtml(book.genre)}</span>
        ${book.pages ? `<span>${book.pages} pages</span>` : ''}
        ${book.yearRead ? `<span>Read ${book.yearRead}</span>` : ''}
      </div>
    </header>
    <div class="article-content">
      ${note}
      ${renderArticleBody(content)}
      ${book.favoriteQuote ? `
        <aside class="pull-quote">
          <span class="quote-label">Favourite quote</span>
          "${escapeHtml(book.favoriteQuote)}"
        </aside>` : ''}
    </div>
  `;

  const prevIndex = index > 0 ? index - 1 : null;
  const nextIndex = index < books.length - 1 ? index + 1 : null;

  if (prevIndex !== null) {
    prevLink.hidden = false;
    prevLink.href = `#book/${prevIndex}`;
    prevLink.querySelector('span').textContent = books[prevIndex].title;
  } else {
    prevLink.hidden = true;
  }

  if (nextIndex !== null) {
    nextLink.hidden = false;
    nextLink.href = `#book/${nextIndex}`;
    nextLink.querySelector('span').textContent = books[nextIndex].title;
  } else {
    nextLink.hidden = true;
  }
}

function showLibrary() {
  articleView.hidden = true;
  libraryView.hidden = false;
  document.title = "Noah's Book Reviews";
}

function showArticle(index) {
  closeModal();
  libraryView.hidden = true;
  articleView.hidden = false;
  renderArticle(index);
  const book = books[index];
  if (book) document.title = `${book.title} — Noah's Book Reviews`;
  window.scrollTo({ top: 0, behavior: 'instant' in window ? 'instant' : 'auto' });
}

function handleRoute() {
  const hash = window.location.hash;
  const match = hash.match(/^#book\/(\d+)$/);
  if (match) {
    showArticle(Number(match[1]));
  } else {
    showLibrary();
  }
}

function closeModal() {
  modal.hidden = true;
  modal.setAttribute('aria-hidden', 'true');
  document.body.style.overflow = '';
}

grid.addEventListener('click', e => {
  const card = e.target.closest('.book-card');
  if (card) openModal(Number(card.dataset.index));
});
grid.addEventListener('keydown', e => {
  if (e.key !== 'Enter' && e.key !== ' ') return;
  const card = e.target.closest('.book-card');
  if (card) { e.preventDefault(); openModal(Number(card.dataset.index)); }
});

modal.addEventListener('click', e => {
  if (e.target.matches('[data-close]')) closeModal();
});
document.addEventListener('keydown', e => {
  if (e.key === 'Escape' && !modal.hidden) closeModal();
});

[searchInput, genreFilter, ratingFilter, sortSelect].forEach(el =>
  el.addEventListener('input', applyFilters)
);

window.addEventListener('hashchange', handleRoute);

populateGenres();
applyFilters();
handleRoute();
